#include <stdio.h>

int main() {
    int count = 1;  // Initialization

    while (count <= 5) {  // Condition
        printf("Count: %d\n", count);
        count++;  // Increment
    }

    return 0;
}
