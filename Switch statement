

#include <stdio.h>

int main() {
    int choice;
    printf("Enter a number (1-3): ");
    scanf("%d", &choice);

    if (choice == 1) {
        printf("You chose option 1.\n");
    } else if (choice == 2) {
        printf("You chose option 2.\n");
    } else if (choice == 3) {
        printf("You chose option 3.\n");
    } else {
        printf("Invalid option.\n");
    }

    return 0;
}

Using switch Statement

#include <stdio.h>

int main() {
    int choice;
    printf("Enter a number (1-3): ");
    scanf("%d", &choice);

    switch (choice) {
        case 1:
            printf("You chose option 1.\n");
            break;
        case 2:
            printf("You chose option 2.\n");
            break;
        case 3:
            printf("You chose option 3.\n");
            break;
        default:
            printf("Invalid option.\n");
    }

    return 0;
}

In both examples:

The program prompts the user to enter a number.

It then checks the input value and prints a corresponding message.

The if-else if or switch statement handles multiple cases, each representing a different decision path based on the value of choice.


